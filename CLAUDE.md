# Compatibility Notice

This repository now uses `AGENTS.md` as the only active control file for Codex.

If a legacy workflow expects `CLAUDE.md`, read `AGENTS.md` instead.

Do not maintain duplicate instructions here.
