# Claude Code local settings
.claude/

# Build output
bin/
obj/
build.log
.dotnet/
.dotnet-cli/
.nuget/

# Decompiled game code (large, user-specific)
decompiled/

# OS files
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.vs/
.vscode/
*.user
*.suo

# Internal notes (not part of template)
docs/claude-md-improvements.md

# Local files
age-of-decadence-analyse.txt
nul
*.zip
artifacts/release-staging/

# Development tools (extraction tool, build artifacts, dependencies)
tools/

# Local tool caches
*.lscache
artifacts/navigation/mesh_guid_index.cache.json

# Per-cell vertical-span blocker export (148MB intermediate, regenerable via
# Export-SceneBlockerData.ps1; the consumed output is navigable_region.bake.json,
# which is committed). Over GitHub's 100MB per-file push limit, so not tracked.
artifacts/navigation/thirdpersongreybox-blockers.COLUMNS.json

# Python bytecode cache
**/__pycache__/
*.pyc

# Coverage-sweep harness output (reproducible from sweep_coverage_planner.py)
artifacts/navigation/sweep/

# Local debug spew from exporter / reachability probe runs
artifacts/navigation/_*.log
artifacts/navigation/_*.txt
