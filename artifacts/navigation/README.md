# Navigation Asset Findings

This folder contains asset-mined navigation inputs from the ripped `ThirdPersonGreybox.unity` scene.

## Files

- `thirdpersongreybox-navigation-data.json`
  - Generated by `.\scripts\Export-SceneNavigationData.ps1`
  - Includes serialized `CameraSpaces`, door-like scene objects with world positions, camera objects, and teleporter endpoints

## Regeneration

Run:

```powershell
.\scripts\Export-SceneNavigationData.ps1
```

## What We Have

- All zones used by the current game-side `navigation_graph.json`
- Extra `CameraSpaces` variants that can serve as intermediate or doorway-adjacent anchors
- One authored teleporter transition for crawlspace with explicit endpoints and rotations
- Door and door-camera scene objects with world positions for the house
- Enough `CameraSpaces` position and scale data to derive source-side and destination-side crossing anchors for every open-passage link in the current graph

## Candidate Authored Connectors

- `hallway -> office`
  - Door object: `Doors_Office`
  - Door cameras: `Camera_DorianOfficeDoor1`, `Camera_DorianOfficeDoor2`

- `office -> office_closet`
  - Door cameras: `Camera_DorianOfficeClosetDoor1`, `Camera_DorianOfficeClosetDoor2`
  - Supporting room camera: `Camera_OfficeCloset`

- `office -> crawlspace`
  - Teleporter object: `CrawlspaceLadder`
  - Authored endpoints: `Crawlspace Enter Position`, `Crawlspace Exit Position`
  - Supporting door cameras: `Camera_DorianTrapdoor1`, `Camera_DorianTrapdoor2`

- `upper_hallway -> bedroom`
  - Door object: `Doors_Bedroom`
  - Door cameras: `Camera_DorianBedroomDoor1`, `Camera_DorianBedroomDoor2`

- `bedroom -> bedroom_closet`
  - Door cameras: `Camera_DorianBedroomClosetDoor1`, `Camera_DorianBedroomClosetDoor2`
  - Supporting room camera: `Camera_BedroomCloset`

- `bedroom -> bathroom2`
  - Door object: `Doors_Bedroom_Bathroom`
  - Door cameras: `Camera_DorianBathroom2Door1`, `Camera_DorianBathroom2Door2`

- `hallway -> bathroom1`
  - Door object: `Doors_Bathroom1`
  - Door cameras: `Camera_DorianBathroomDoor1`, `Camera_DorianBathroomDoor2`

- `upper_hallway -> gym`
  - Door object: `Doors_Gym_Hall`
  - Door cameras: `Camera_DorianGymDoor1`, `Camera_DorianGymDoor2`

- `gym -> gym_closet`
  - Door cameras: `Camera_DorianGymClosetDoor1`, `Camera_DorianGymClosetDoor2`
  - Supporting room cameras: `Camera_Gym_Closet`, `Camera_Gym_Closet2`, `Camera_Gym_Closet3`

- `upper_hallway -> attic`
  - Door object: `AtticDoors`
  - Door cameras: `Camera_DorianAtticDoor1`, `Camera_DorianAtticDoor2`

- `upper_hallway -> hallway`
  - Stair cameras: `Camera_StairsUp`, `Camera_StairsDown`

- `laundry_room -> laundry_room_closet`
  - Door camera: `Camera_DorianLaundryCloset1`
  - Supporting room camera: `Camera_Laundry Room_Closet`

## Links That Are Probably Open Passages, Not Doors

- `living_room_tutorial -> living_room`
- `hallway -> living_room`
- `living_room -> dining_room`
- `dining_room -> piano_room`
- `dining_room -> kitchen`

These links now derive their waypoints and crossing anchors from the scene's own `CameraSpaces` data instead of from hand-picked room-pair overrides.

Current open-passage build strategy:

- For each graph link marked `OpenPassage`, collect the exact scene zone plus any same-family numbered `CameraSpaces` variants such as `hallway2` to `hallway7`.
- Evaluate every source and destination candidate pair using the serialized zone bounds when present, or the scene point position when the zone has no bounds.
- Pick the pair with the smallest boundary gap, then emit:
  - `FromWaypoint` and `ToWaypoint` from the selected source and destination scene-zone positions
  - `FromCrossingAnchor` and `ToCrossingAnchor` from the closest boundary points between those selected scene zones
- This now covers the six major room-to-room passages and the remaining subzone-to-room open-passage links such as `dorian_office2 -> office` and `kitchen_ronald -> kitchen`.

## Practical Build Order

1. Use teleporter endpoints directly for `office -> crawlspace`.
2. Use paired door cameras for links with obvious `Camera_Dorian...Door...` assets.
3. Use stair cameras for `upper_hallway -> hallway`.
4. Use `CameraSpaces` geometry to derive open-passage anchors, then only add hand-authored overrides if runtime testing proves a specific link still needs them.
